//! Verifier-owned adoption of the packet's real-daemon profile administration
//! cases. The source remains beside its fixture support so the same user-level
//! checks run from the independent invariants lane.

#[path = "../../crates/jinnd-daemon/tests/profile_admin.rs"]
mod profile_admin;
