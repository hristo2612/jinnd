# Packet M2-K1 — Harness-era cleanup: lift lane/slot host machinery into `jinnd-wasm`

**Milestone:** M2 (kernel-side entry debt from the M1 close decision log) ·
**Owner:** kernel-dev · **Status:** ready · **Binding rules:** R1, R5, R10,
R11, Law 1 · **LOC ceilings (card-authoritative):** `crates/jinnd-daemon` back
to ≤650 after the lift · `crates/jinnd-wasm` growth ≤500 (the lifted
machinery) · net workspace delta ≤ +50 (this is a MOVE with dedup, not a
feature) · adapter delta ≤100 (dedup only).

Accepted-deferred at M1-P9 round 1 (PLA-283 ruling wic_f9fb04fd17c4): the
daemon accumulated ~430 LOC of generic wasm package-lane + swap-slot machinery
plus the jinn:fs host provider because Law 1 forbade sharing them with the
harness-lane adapter. The right home for production host machinery is the
production host crate.

## Scope

- Lift the generic, lane-agnostic parts of the daemon's wasm package lane,
  seat/slot machinery, and the jinn:fs host provider into `jinnd-wasm` as
  production helpers with a clean seam; `jinnd-daemon` shrinks back toward a
  shell (assembly + watching + shutdown only).
- De-duplicate the adapter against the lifted helpers ONLY where the code is
  genuinely lane-generic — anything whose sharing would re-open a Law 1 side
  door stays duplicated, with a one-line comment naming the law.
- **Zero behavior change.** The headless 5-step demo test, the full invariant
  suite (95/35 ratchet exact), and every crate suite are the pin — no output,
  ledger sequence, or lifecycle observable may move.

## Acceptance

- Ratchet byte-identical (cases=130 expected-green=95 expected-red=35);
  headless demo test green; real-binary smoke boot + SIGINT clean.
- jinnd-daemon ≤650 by the canonical meter; every src file under 300 lines;
  no new public facade surface (R12: this packet adds NOTHING to contracts).
- Gates: fmt, clippy -D warnings, build, test, ratchet — real tails; loom
  lanes unchanged and green; miri on touched non-FFI crates.
- Two-key: zero touches under tests/invariants/.

## Out of scope

Any semantic change, any contract/WIT change, C5/C6 (held for cron's consumer
evidence), Tier B, anything the move does not strictly require.
