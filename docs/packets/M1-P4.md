# Packet M1-P4 — Service Registry & Epochs (`jinnd-registry`) + adapter wiring

**Milestone:** M1 · **Owner:** kernel-dev · **Status:** ready ·
**Binding rules:** R1, R3, R4, R5, R9, R10, R11 · **LOC budget:** 500–700 for
`crates/jinnd-registry`; adapter wiring delta in `crates/jinnd-adapter` capped at
250 additional LOC (counted separately). Metric: src LOC excluding cfg(test),
blanks, comments.

> **AMENDED 2026-08-25 (COO adjudication, round-1 escalation):**
> 1. **Facade delta authorized and named**: an additive dependency-declaration /
>    injection surface on `jinnd-api` (declare injected service contracts on a
>    plugin; resolve them at activation) — the minimum needed for the
>    provide/inject + reactive-readiness acceptance. Additive only; document the
>    exact items in the report.
> 2. **Red-reason acceptance restated**: every remaining red fails AT A DESIGNED
>    SITE with its RECORDED reason — `NO_KERNEL` for unwired subsystems
>    (events, loader), `FACADE_GAP` for cases whose probes now pass but whose
>    assertions need later packets. The FACADE_GAP transition of the 67 wired-
>    subsystem cases is the designed P0b "red through the adapter" path (per the
>    PLA-255 ruling, the verifier ships the ratchet reason-class change in the
>    same packet: per-case recorded-reason catalog, ratchet-enforced).
> 3. **LOC budgets are ceilings, not floors** (registry ≤700; adapter delta
>    ≤250). A leaner implementation that passes acceptance is R10-correct.

**This is the greening packet** — the first packet whose acceptance includes
invariant cases turning green (COO ruling on PLA-255).

## Scope

### `crates/jinnd-registry`
- Typed service slots keyed by the context crate's `ServiceKey` (R3); resolution
  honors context isolation boundaries (realm lookup via `jinnd-context`).
- Registration/provision are **effects** on the owning fiber's scope (R5) — a
  provider's slot disappears through undo replay, never through ad-hoc mutation.
- **Reactive availability** via `watch` (never polled, R1), implementing the
  readiness trait `jinnd-fiber` abstracted in P3: a consumer fiber activates only
  when every injected provider is Active and passes its check.
- **Epoch gating**: a consumer's epoch encodes the generation/uid of every
  injected provider. Any provider change — including hot-swap — changes the
  epoch and forces the consumer through a full clean unload → reload. There is
  no silent replace, ever (R9).
- **Provider-waits-for-consumers disposal** (I2 semantics): a dying provider
  unregisters its slot and notifies first, then awaits all dependents' unload
  completion before its value drops — dependents may still call the dying
  service during their own teardown.

### `crates/jinnd-adapter` wiring
- Implement `kernel()` over context + effects + fiber + registry: spawn / update /
  restart / wait_for_quiescence / state and the service provide/inject paths.
- Wire ONLY subsystems that exist. Events/loader/etc. keep their `todo!` stubs
  with their **exact current reason strings**. If any red case's failure reason
  would change without the case going green, STOP and report — the verifier
  rules; the ratchet enforces original reasons.

### Greening protocol (two-key)
- List EXACTLY which invariant cases turn green — start from the P3 round-1
  candidates (`cordis_fiber::config_update_restarts_with_each_latest_config`,
  `cordis_fiber::explicit_restart_reactivates_once_and_ends_active`) plus any
  context/effects/registry cases now executable end-to-end.
- You do NOT touch `tests/invariants/` or `expected-green.txt`. The verifier
  validates every claimed green independently and updates the ratchet on the
  branch; a green the verifier can't reproduce is a blocker on the claim, not
  on the code.

### Concurrency evidence
- Loom models for any interleaving-sensitive registry path (registration vs
  disposal vs epoch change); per AGENTS.md, lifecycle concurrency claims need a
  model exercising the claimed interleaving.

## Facade

Minimal **additive** delta authorized if the adapter needs registry-facing
types — documented in the report with the exact items. Prefer zero (R12).

## Acceptance

- **Nonzero verified green invariant cases**, each validated by the verifier,
  `expected-green.txt` updated by the verifier, and
  `tests/invariants/check-ratchet.sh` green with the new expected-green set.
- Every remaining red case fails at a designed site with its recorded reason
  (`NO_KERNEL` or `FACADE_GAP`), per the amended clause above.
- Gates: fmt, clippy -D warnings, build, test, ratchet — paste real tails.
- Loom suite green where models exist (paste tail); miri clean on the new crate
  (exclusions listed with reasons).
- LOC within both budgets; largest file under 300 lines (R10).
- Two-key: zero touches under `tests/invariants/`.

## Out of scope

Event bus, profile loader, ledger, wasm host, any test file, any facade change
beyond the authorized additive delta.
