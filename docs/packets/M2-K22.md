# M2-K22 — the contract index and a bundle header cite versions the bundles no longer carry, and gate 1 cannot see them (harness FINDINGS #44)

**Milestone:** M2 — instrument follow-on to M2-K16 · **Owner:** kernel-dev ·
**Status:** ready · **Todo:** PLA-346 · **Binding rules:** R10, R12, Laws 1/2 ·
**Design decisions in scope: ONE** — how the index and world mentions become
derived or gated · **Facade:** NO `jinnd-api` delta · **Size:** declare with
`tools/loc-meter`; the lens is dev-only, production 0; any number you declare
is an estimate unless this card sets a ceiling (it does not) · **Standing
gates:** all usual + `cargo build --workspace --locked`.

## The defect

Found by harness pin-bump 6 (PLA-344) at `85d36b4`, FINDINGS #44:
`contracts/README.md` (the contract index) names jinn-net 0.1.0 and
jinn-introspect 0.1.0 while the bundles are 0.3.0 and 0.5.0; and
`contracts/jinn-net/contract.wit:9` cites plugin world 0.9.0 while the world is
0.10.0. Same class as #43 — a hand-maintained second copy of a fact the code
knows — and M2-K16's gate 1 saw neither, by its stated limits: the index is
Markdown outside gate 1's surface, and "plugin world 0.9.0" is a world mention,
not a `jinn:x@X.Y.Z` reference.

## What to build

1. Correct both citations.
2. Widen gate 1 in `jinnd-contract-lens`: (a) the contract index's version
   table is DERIVED from the parsed bundles or checked against them — prefer
   derived (the record moves with the thing); (b) a bare "plugin world X.Y.Z"
   mention in `wit/` and `contracts/` is checked against `wit/plugin.wit`'s
   parsed package. Both readers are single-home, grammar stated, FAIL CLOSED
   on an unreadable candidate (the M2-K16 ruling).
3. Red-first against the two live defects, output shown; sweep every bundle
   and report the count, zero included with output.

## Out of scope

Anything beyond the index and world mentions; no runtime change.

## Evidence standard — inherited

Checkable red-first; one mutation per reader with file:line, exact command and
verbatim failing output on the Todo; `pass` = 0 Blockers; a ruling-mandated
overrun is MANDATED; the repo is PUBLIC. STOP RULE: 2 rounds.
